{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug MediaPlayer (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/MediaPlayer",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: build",
      "terminal": "integrated",
      "env": {
        "QT_LOGGING_RULES": "qt.multimedia.*=true"
      }
    }
  ]
}